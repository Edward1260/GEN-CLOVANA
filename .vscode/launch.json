{
    "configurations": [
    {
        "name": "Connect to server",
        "type": "go",
        "request": "attach",
        "mode": "remote",
        "remotePath": "${workspaceFolder}",
        "port": 2345,
        "host": "127.0.0.1"
    }
    ]
}Great! Your launch.json is set up to launch Chrome against http: //localhost:8080 for debugging. If you need to add more configurations (for example, for Node.js or other browsers), let me know!
{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Launch Chrome against localhost",
      "url": "http://localhost:8080",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
